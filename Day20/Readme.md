# Question 

input - [ 1,2,2,3,3 ]
output - 1

- Bitwise XOR working
1. Same value gives 0 - 1 XOR 1 = 0, 0 XOR 0 = 0
2. Different value gives 1 - 1 XOR 0 = 1
3. It is used for removing duplicates in the array

# Problem Pattern

1. For solving the problem with O(n) and O(1), time & space complexity
2. We need to use Bitwise XOR


# Complexity
- O(n) - Linear

# Time Complexity
- O(1) - Constant

=======================================================================

# Question 

input - [ 1,2,2,3,3 ]
output - 1

# Problem Pattern

1. Use an obj to and travesre the entire array - Using bracket Notation for this
2. Use key value pair to store the frequency of values count


# Complexity
- O(n) - Linear

# Time Complexity
- O(n) - Linear
- why space becomes O(n)?
- Because we are using an obj to store the arrays in the form of key value pairs