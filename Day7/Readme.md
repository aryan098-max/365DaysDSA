# Pattern Question

            *
          * *        
        * * * 
      * * * *
    * * * * *  

# Solving Pattern

1. You have to use three loops here
2. First Loop for columns
2. Second Loop for spaces 
3. Third Loop for stars

# Complexity 

1. Time Complexity
- O(n) * O(n) + O(n) 
= O(n) * (O(n) + O(n))
= O(n) * 2O(n)
= O(n)


2. Space Complexity
- O(1) - Constant